import React from "react";

const Header = () => {
    return (
        <header className='flex p-8 justify-center'>
            <h1 className="text-4xl font-[''] app-name">Pack´n check!</h1>
        </header>
    );
};

export default Header;
