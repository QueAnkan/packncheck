import Navigation from "./ListLinks";

const Footer = () => {
    return (
        <footer className='fixed bottom-0 w-full lg:hidden'>
            <Navigation />
        </footer>
    );
};

export default Footer;
